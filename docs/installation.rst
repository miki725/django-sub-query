============
Installation
============

At the command line::

    $ easy_install django-sub-query

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-sub-query
    $ pip install django-sub-query
